# LyricQuizGame
Quiz Game that pulls 2 random lines of lyrics from each song in a song album file and allows the user to guess which song the lyrics are from. The options are given as a drop down menu from the song names that are also pulled from the file names. Currently only has one test album. 
